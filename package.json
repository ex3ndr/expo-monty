{
  "name": "monty-js",
  "private": true,
  "license": "MIT",
  "packageManager": "yarn@1.22.22",
  "workspaces": {
    "packages": [
      "expo-monty"
    ],
    "nohoist": [
      "**/react",
      "**/react-dom",
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "build": "yarn build:web && yarn build:expo",
    "build:expo": "yarn workspace expo-monty build",
    "build:web": "yarn workspace expo-monty build:web-runtime",
    "clean": "yarn workspaces run clean"
  }
}
