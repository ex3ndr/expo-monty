{
  "name": "expo-monty",
  "version": "0.1.0",
  "description": "Nitro Modules bridge for Monty on iOS, Android, and web",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "react-native": "src/index.ts",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "default": "./build/index.js"
    }
  },
  "source": "src/index.ts",
  "files": [
    "android",
    "ios",
    "cpp",
    "nitrogen",
    "src",
    "build",
    "nitro.json",
    "MontyExpo.podspec",
    "post-script.js",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/copy-web-runtime.mjs",
    "build:web-runtime": "node ./scripts/build-web-runtime.mjs",
    "codegen": "nitrogen --logLevel=\"debug\" && node post-script.js",
    "build:rust": "bash ./scripts/build-rust-all.sh",
    "build:rust:ios": "bash ./scripts/build-rust-ios.sh",
    "build:rust:android": "bash ./scripts/build-rust-android.sh",
    "build:native": "yarn build:rust && yarn codegen && yarn build",
    "build:release": "yarn clean && yarn build:web-runtime && yarn build:native",
    "clean": "rm -rf build",
    "prepack": "yarn build:release",
    "prepare": "yarn build"
  },
  "keywords": [
    "nitro",
    "react-native",
    "monty",
    "python",
    "module"
  ],
  "config": {
    "montyUpstreamRef": "v0.0.7"
  },
  "dependencies": {
    "@napi-rs/wasm-runtime": "^1.1.1"
  },
  "devDependencies": {
    "nitrogen": "0.35.0",
    "react": "19.2.0",
    "react-native": "0.82.1",
    "react-native-nitro-modules": "0.35.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*",
    "react-native-nitro-modules": "*"
  }
}
